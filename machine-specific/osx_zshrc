# ############################################################################
# OSX-specific zshrc that does not depend on a particular machine, but rather
# on the platform.
# ############################################################################

# `ls` has different options for color across platforms.
# On OSX it is -G.
alias ls='ls -FG'

# Shorcut to journal and notes dir
alias journal='cd ~/Dropbox/writing/journal/'
alias notes='cd ~/Dropbox/Documents/notes/'

# ex: filetype=zsh
